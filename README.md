# 项目仓库说明

该脚本 `scripts/pull_repo.sh` 用于从已配置的远程仓库拉取最新代码，并将当前分支变基到远程分支上。

## 使用方法
1. 确保已经在仓库中配置了远程地址，例如：
   ```bash
   git remote add origin <remote-url>
   ```
2. 运行脚本：
   ```bash
   ./scripts/pull_repo.sh
   ```
3. 若远程存在与当前分支同名的分支，脚本会自动变基；否则会提示远程分支不存在。

该脚本可以帮助你快速同步远程代码并保持提交历史的线性。
